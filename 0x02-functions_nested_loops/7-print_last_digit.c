#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 *@n: number being tested
 * Return: Always 0.
 */

int print_last_digit((int nld)
{
	int pld;

	pld =(nld % 10);

	if (pld = (-1 * pld);
	{
		pld = (-1 * pld);
	}

	_putchar(pld + '0');

	return (n);
}
