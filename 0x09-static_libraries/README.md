C - Static libraries

Learning Objectives 

1. What is static librarary, how does it work, how to create static libraries, how to use static Libraries

2. Use of ar, ranlib and the nm commands in static libraries 

3. Shared or Dynamic libraries.

4. For the tasks, the below are the requirements
	. Syntax and style should conform to the Betty style coding
	. All files are created and compiled on ubuntu 14.04.4 LTS on gcc 4.8.4 	  using the flags -Wall -Werror -Wextra and -pedantic

5. A library is not a luxury but one of the necessities of life
Create the static library libmy.a

6. Without libraries what have we? We have no past and no future
Create a script called create_static_lib.sh that creates a static library called liball.a from all the .c files that are in the current directory. 
